# Fixed requirements.txt - Railway optimized
openai==1.9.0
numpy
scipy
deepl
pydub
cohere
ffmpeg-python

# Use CPU-only PyTorch to save memory and avoid conflicts
--index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchaudio==2.5.1+cpu

# Lighter TensorFlow alternative or remove if not needed
# tensorflow-probability==0.23.0  # Comment out if not essential
typing-extensions>=4.10.0,<5