// server/utils/answer-ai.ts
export async function generateAnswer(question: string): Promise<string> {
  // Replace with real AI call if needed
  return `This is a placeholder response for: ${question}`
}
