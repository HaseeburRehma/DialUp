# etc/asterisk/http.conf
[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088   ; plain WS only

; No TLS here — nginx handles termination
tlsenable=no

; Allow all origins (or restrict later)
allowed_origins=*

